---
sidebar_position: 1
---

# Introduction

**Email Link Host** is a free, static (or tiny Go/Docker) host for:

- Firebase Auth **email-link** continue URLs on a branded domain
- Android **App Links** (`/.well-known/assetlinks.json`)
- iOS **Universal Links** (`/.well-known/apple-app-site-association`)
- Optional Google Play / App Store buttons on a themed landing page

It pairs with **[App Universal Links Helper](https://rutvik24.github.io/app-universal-links-helper/)** ([source](https://github.com/rutvik24/app-universal-links-helper)) — a browser tool that generates association files and AndroidManifest snippets you can paste into this host.

## Quick paths

| Goal | Start here |
| --- | --- |
| Configure brand / theme / store URLs | [Configuration](/docs/email-link-host/configuration) |
| Deploy with Docker | [Docker hosting](/docs/hosting/docker) |
| Deploy serverless (Firebase, Cloudflare, Vercel, …) | [Hosting overview](/docs/hosting/overview) |
| Generate association files | [Universal Links Helper](/docs/universal-links-helper/overview) |
| Let an AI agent host for you | [Agent skills](/docs/agents/overview) |

## Stack

- **Next.js** static export for CDN hosts (Bun for install/build)
- **Pure Go** binary for Docker (`scratch` image, multi-arch on Docker Hub as `$DOCKERHUB_USERNAME/email-link-host`)
- **Docusaurus** docs (this site), always with **Bun** — not npm/yarn

## Always use Bun

```bash
bun install
bun run dev
bun run build
```

In the docs site:

```bash
cd docs-website
bun install
bun start
```
