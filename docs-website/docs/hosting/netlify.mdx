---
sidebar_position: 6
---

# Netlify

`netlify.toml` sets build `bun run build`, publish `out`, and JSON headers for association files.

Connect the repo or:

```bash
bun run build
bunx netlify deploy --prod --dir=out
```
