---
sidebar_position: 4
---

# Cloudflare Pages

- Build command: `bun run build`
- Output directory: `out`
- Headers: `public/_headers` (copied into `out/`)

```bash
bun run build
bunx wrangler pages deploy out
```

See `wrangler.toml` (`pages_build_output_dir = "out"`).
