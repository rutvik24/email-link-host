---
sidebar_position: 7
---

# AWS Amplify

`amplify.yml` installs with Bun, builds to `out`, and sets `customHeaders` for `.well-known` files.

Connect the GitHub repo in the Amplify console and use the checked-in `amplify.yml`.
